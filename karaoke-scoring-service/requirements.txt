# ==============================================================================
# REQUIREMENTS - KARAOKE SCORING SERVICE
# ==============================================================================
# NOTE ON DEPLOYMENT ARCHITECTURE:
# Due to the package size of librosa, scipy, numpy, soundfile, and psycopg2-binary
# (~250MB+ uncompressed), this Lambda function MUST be deployed as an AWS ECR
# Container Image (NOT a standard zip package deployment).
# ==============================================================================

fastapi>=0.110.0
mangum>=0.17.0
uvicorn[standard]>=0.28.0

# Signal Processing Core Stack
librosa>=0.10.1
numpy>=1.26.0
scipy>=1.12.0
soundfile>=0.12.1

# AWS SDK & Database Persistence
boto3>=1.34.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# Validation & Settings
pydantic>=2.6.0
pydantic-settings>=2.2.0

# Testing
pytest>=8.0.0
httpx>=0.27.0
